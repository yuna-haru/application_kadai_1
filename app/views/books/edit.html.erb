<div class="box">
  <div class="flash">
    <%= flash[:success] %>
  </div>
   <h1>Editing Book</h1>
  <% if @book.errors.any? %>
  　<div class="error">
      <h2 class="error_text"><%= @book.errors.count %> prohibited this book from being saved:</h2>
       <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
         <% end %>
     </ul>
    </div>
  <% end %>

  <div class="form-box">
    <%= form_with model: @book, url: book_path(@book.id), method: :patch do |f| %>
      
      <div class="form_edit">
        <label for="book_title">Title</label><br>
        <%= f.text_field :title %>
      </div>
      <div class="form_edit">
        <label for="book_body">Body</label><br>
        <%= f.text_area :body %>
      </div>
      <div class="form_edit">
        <%= f.submit 'Update Book' %>
      </div>
    <% end %>

    <%= link_to  "Show", book_path,class: "link" %> | 
    <%= link_to  "Back", books_path,class: "link" %>

  </div>
</div>